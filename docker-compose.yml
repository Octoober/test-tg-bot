services:
  whitelist_bot:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PYTHONUNBUFFERED=1
    tty: true
    stdin_open: true
    volumes:
      - ./data:/data
      - .:/workspace
    env_file:
      - .env
    ports:
      - "8080:8080"